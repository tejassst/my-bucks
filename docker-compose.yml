version: '3.8'

services:
  # MongoDB database
  mongodb:
    image: mongo:7.0
    container_name: mybucks-db
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
      MONGO_INITDB_DATABASE: mybucks
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    ports:
      - "27017:27017"
    networks:
      - mybucks-network

  # myBucks Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mybucks-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=4040
      - MONGO_URL=mongodb://admin:password123@mongodb:27017/mybucks?authSource=admin
      - ALLOWED_ORIGINS=http://localhost:4040,http://127.0.0.1:4040
    ports:
      - "4040:4040"
    depends_on:
      - mongodb
    networks:
      - mybucks-network
    volumes:
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4040/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Development database (for local development)
  mongodb-dev:
    image: mongo:7.0
    container_name: mybucks-db-dev
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: mybucks_dev
    volumes:
      - mongodb_dev_data:/data/db
    ports:
      - "27018:27017"
    networks:
      - mybucks-network
    profiles:
      - dev

volumes:
  mongodb_data:
    driver: local
  mongodb_dev_data:
    driver: local

networks:
  mybucks-network:
    driver: bridge

# Development override
---
# docker-compose.override.yml (automatically loaded in development)
version: '3.8'

services:
  # Override for development
  app:
    build:
      target: builder
    command: npm run dev
    environment:
      - NODE_ENV=development
      - MONGO_URL=mongodb://mongodb-dev:27017/mybucks_dev
      - REACT_APP_API_URL=http://localhost:4040/api
    volumes:
      - .:/app
      - /app/node_modules
    profiles:
      - dev
